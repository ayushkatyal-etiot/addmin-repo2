{
  "name": "addmin",
  "version": "0.1.0",
  "private": true,
  "description": "AddMin — office administration operating system. pnpm monorepo (apps/web, apps/api, packages/db).",
  "scripts": {
    "dev": "pnpm --parallel --filter \"./apps/**\" run dev",
    "build": "pnpm --filter \"./packages/**\" run build && pnpm --filter \"./apps/**\" run build",
    "lint": "pnpm --filter \"./apps/**\" run lint",
    "typecheck": "pnpm --filter \"./packages/db\" run build && pnpm --recursive run typecheck",
    "test": "pnpm --filter \"./apps/**\" run test"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.3",
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
