{
  "name": "@addmin/db",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Prisma schema and typed client for AddMin (apps/web, apps/api).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "build": "prisma generate",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"@addmin/db: no lint configured yet\""
  },
  "dependencies": {
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "prisma": "^5.20.0",
    "typescript": "^5.6.3"
  }
}
